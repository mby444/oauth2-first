// import { Router } from "express";

// export const errorRoute = Router();

// errorRoute.get("/", (req, res) => {
//   const { message = "Error" } = req.query;
//   res.status(500).send(message);
// });
// errorRoute.all("*", (req, res) => {
//   res.sendStatus(404);
// });
